# Estrutura do Banco de Dados – Projeto Estoque

Este repositório contém as migrations SQL responsáveis por criar a estrutura
do banco de dados utilizada pelo sistema de controle de estoque.

## Como usar

1. Crie o banco de dados (caso ainda não exista):

```sql
CREATE DATABASE estoque CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
